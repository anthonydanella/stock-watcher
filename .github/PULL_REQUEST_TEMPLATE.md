## Summary

<!-- What does this change do and why? -->

## Test results

<!-- `uv run pytest` for backend changes; `npm run build` / `npm run lint` for frontend changes. -->

- [ ] `uv run pytest` passes (backend changes)
- [ ] `cd frontend && npm run build` passes (frontend changes)
- [ ] Bumped `frontend/package.json` version (user-visible frontend changes)

## Linked issues

<!-- e.g. Closes #123 -->

## Screenshots

<!-- For visible UI changes. -->
