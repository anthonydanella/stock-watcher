# Contributing

## Before opening a pull request

- Bump the version in `frontend/package.json` for user-visible frontend changes.

## Commit messages

Use clear, imperative messages such as `Add scheduler cooldown test`.